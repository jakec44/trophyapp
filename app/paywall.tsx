/**
 * Paywall bypass - renders Fish Room directly.
 * Pro features are unlocked; no upgrade required.
 */
import TrophyRoomScreen from '@/app/(tabs)/trophy-room';

export default function PaywallScreen() {
  return <TrophyRoomScreen />;
}
