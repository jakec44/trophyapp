-- Seed demo tournament entries (DISABLED).
-- Do not auto-enroll real users. Placeholder data is client-side only (ENABLE_MOCK_USERS + getMockTournamentEntries).
-- No-op so migration history is unchanged; 20260331000000_remove_seed_tournament_entries cleans any existing seed data.

SELECT 1;
